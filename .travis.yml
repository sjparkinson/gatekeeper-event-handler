language: python

python:
  - "2.7"

install:
  - pip install -r requirements-dev.txt

script:
  - flake8 app.py

deploy:
  provider: script
  script: chalice deploy
  skip_cleanup: true
  on:
    branch: master
